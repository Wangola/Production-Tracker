/**
 * Room for future implementation currently never used. Allows any class access these predefined
 * enums designed for Screens. A screen can have a MonitorType
 *
 * @author William Angola
 */
public enum MonitorType {
  //Creating new objects (Never Used)
  /**
   * Object (Currently never used).
   */
  LCD(),

  /**
   * Object (Currently never used).
   */
  LED()

}
