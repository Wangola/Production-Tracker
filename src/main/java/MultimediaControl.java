/**
 * Room for future implementation currently never used.Allows classes to contract these methods and
 * implement when needed.
 *
 * @author William Angola
 */
public interface MultimediaControl {

  /**
   * Play something depends on class that implements (currently never used).
   */
  void play();

  /**
   * Stop something depends on class that implements (currently never used).
   */
  void stop();

  /**
   * Previous something depends on class that implements (currently never used).
   */
  void previous();

  /**
   * Next something depends on class that implements (currently never used).
   */
  void next();

}
